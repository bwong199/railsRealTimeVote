An app that allows voting for the most popular programming language. It's built using Rails 4 and Socket.io. The client side sends a socket request to the node server and the node server subsequently broadcasts the vote. 
